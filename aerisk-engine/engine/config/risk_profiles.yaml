version: "1.0"

profiles:
  default:
    risk_score_thresholds:
      low: 0.0
      medium: 500.0
      high: 1500.0

    monte_carlo:
      iterations: 10000
      quantiles:
        - 0.90
        - 0.95
        - 0.99

    audit:
      enabled: true

  conservative:
    risk_score_thresholds:
      low: 0.0
      medium: 300.0
      high: 1000.0

    monte_carlo:
      iterations: 15000
      quantiles:
        - 0.95
        - 0.99

    audit:
      enabled: true
